"use client";

// FreePageBanner removed — all features are now free and open to everyone.
// This file is kept as a no-op to avoid breaking imports.
export default function FreePageBanner() {
  return null;
}
