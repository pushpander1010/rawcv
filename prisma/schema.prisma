// PostgreSQL — set DATABASE_URL in .env or .env.local (see .env.example)

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

// Schema simplified — auth/credits removed. Add models as needed.
